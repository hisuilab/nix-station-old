# Codex 固有設定 — nix-station
# ポリシー本体は docs/ai/policy.md を参照

[permissions]
# 変更禁止パスは docs/ai/protected_paths.txt で管理
allow-file-edit = true
allow-shell = true

[hooks]
pre-commit = ".codex/hooks/policy-gate.py"
