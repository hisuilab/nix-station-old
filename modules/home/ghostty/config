# =============================================================================
# Ghostty Config
# =============================================================================

# -----------------------------------------------------------------------------
# Appearance
# -----------------------------------------------------------------------------
theme = Kanagawa Wave
font-family = MesloLGL Nerd Font
font-size = 16
background-opacity = 0.7
window-padding-x = 8
window-padding-y = 8
cursor-style-blink = false

# -----------------------------------------------------------------------------
# Window
# -----------------------------------------------------------------------------
initial-window = false
quit-after-last-window-closed = false
working-directory = inherit
copy-on-select = clipboard

# -----------------------------------------------------------------------------
# macOS
# -----------------------------------------------------------------------------
macos-titlebar-style = tabs
macos-titlebar-proxy-icon = hidden
macos-option-as-alt = left

# -----------------------------------------------------------------------------
# Quick Terminal
# -----------------------------------------------------------------------------
quick-terminal-position = top
quick-terminal-size = 75%

# -----------------------------------------------------------------------------
# Keybinds
# -----------------------------------------------------------------------------
# ghostty +list-keybinds  →  有効なキーバインド一覧
#
# Modifier keys:
#   super = Command (⌘)   alt = Option (⌥)
#   ctrl  = Control (⌃)   shift = Shift (⇧)

# Fullscreen
keybind = super+enter=toggle_fullscreen

# Quick terminal (global shortcut)
keybind = global:ctrl+enter=toggle_quick_terminal

# Pane management is handled by tmux
